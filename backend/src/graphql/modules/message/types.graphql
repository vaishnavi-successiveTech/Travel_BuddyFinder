 type Message {
    _id: ID!
    sender: User!       #  instead of just ID
    recipient: User! 
    content: String!
    read: Boolean!
    createdAt: String!
  }

type User {
  _id: ID!
  name: String
  email: String
  avatar: String
}